var numWays = function(amount, denominations){
	// make sure denominations is sorted?
	var total;
	var numberOfWays;
	for (var i = 0; i < denominations.length; i++){
		var current = denominations[i];
	}
};